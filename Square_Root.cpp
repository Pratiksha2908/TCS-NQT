#include <iostream>
#include <math.h>
#include <bits/stdc++.h>
using namespace std;

int main() 
{
   int n;
   double root;
   cin >> n;
   root = sqrt(n);
   cout << fixed << setprecision(2) << root;
}